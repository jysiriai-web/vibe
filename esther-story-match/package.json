{
  "name": "esther-story-match",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "parse": "node scripts/parseCSV.js",
    "match": "node scripts/match.js",
    "review": "node scripts/review-server.js",
    "dashboard:dev": "cd dashboard && npm run dev",
    "dashboard:build": "cd dashboard && npm run build",
    "update": "node scripts/match.js && cd dashboard && npm run build && vercel deploy out --prod",
    "agent": "node scripts/deploy-agent.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "csv-parse": "^6.2.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "googleapis": "^144.0.0"
  }
}
