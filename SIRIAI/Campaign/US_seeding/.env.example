# 이 파일은 템플릿입니다. 실제 값은 .env 에 넣으세요 (.env 는 git 에 안 올라갑니다).
# 없어도 도구는 정상 동작합니다 — 틱톡이 계속 막을 때만 쓰는 선택 항목입니다.

# 스캐너를 특정 프록시(다른 나라)로 내보내기.
# 시스템 VPN 을 켜두면 크롬이 알아서 그 연결을 타므로 보통은 불필요합니다. 이건 '스캐너만' 프록시로 보낼 때.
#   예) TIKTOK_PROXY=http://호스트:포트   또는  http://아이디:비번@호스트:포트   또는  socks5://호스트:포트
TIKTOK_PROXY=
